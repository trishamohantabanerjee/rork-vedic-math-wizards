# Copy this file to `.env` (or `.env.local`) and fill values.
# Expo automatically exposes only variables prefixed with EXPO_PUBLIC_ to the client.

EXPO_PUBLIC_API_BASE_URL=https://zpdmapgeueiiioyjrxjx.supabase.co
EXPO_PUBLIC_ENABLE_TIMER=true
EXPO_PUBLIC_DEFAULT_QUESTION_COUNTS=10,20,30,40
EXPO_PUBLIC_SUPPORT_EMAIL=support@yourdomain.com

# Private (not auto-exposed). Keep only in local/CI env, never commit real secrets.
# API_SECRET=
